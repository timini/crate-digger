[package]
name = "cd-protocol"
description = "Versioned request and response types shared by Crate Digger and its central service"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
